[package]
name = "crowdit-crowdfund"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.8"
reward-token = { path = "../reward-token" }

[dev-dependencies]
soroban-sdk = { version = "22.0.8", features = ["testutils"] }
